<!DOCTYPE html>
<html>
  <head>
    <meta chatset="utf-8">
    <title>Mi cuenta</title>
  </head>
  <body>
    <p>Editar información de la cuenta:</p>
    <form method="post" action="/user/<%= user.id %>?_method=put" >      
      <% include _form.ejs %>
    </form>
    <form method="post" action="/user/<%= user.id %>?_method=delete">
        <button type="submit" onClick="return confirm('Confirmación para borrar cuenta: <%= user.username %>');">
        Eliminar cuenta
        </button>
    </form>
  </body>
</html>