<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Estadísticas</title>
    <!-- Custom styles -->
    <style>
        ul {margin:0; padding:0;}
        ul li{ list-style:none; margin-bottom:5px;}
    </style>
  </head>
  <body>
    <ul>
      <% if (session.user) { %>
      <li>Favoritos: <%= favoritos %> </li>
      <% } %>
      <li>Número de preguntas: <%= preguntas %></li>
      <li>Número de comentarios publicados: <%= comentarios %></li>
      <li>Número medio de comentarios por pregunta: <%= media.toFixed(2) %></li>
      <li>Número de preguntas sin comentarios: <%= sinComments %> </li>
      <li>Número de preguntas con comentarios: <%= conComments %> </li>
    </ul>
  </body>
</html>
